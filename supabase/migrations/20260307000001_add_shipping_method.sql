-- ============================================================
-- 💡 사장님 추가 지시사항 반영 (출하 방식 추가 등)
-- ============================================================

-- ─────────────────────────────────────────────────────────────
-- STEP 1: shipping_orders 테이블에 shipping_method 추가
-- ─────────────────────────────────────────────────────────────
-- 출고 방식 (택배 발송, 컨테이너 출고, 납품차량 출고)
ALTER TABLE public.shipping_orders
  ADD COLUMN IF NOT EXISTS shipping_method VARCHAR(50);

-- tracking_number는 UI에서 제거하므로 놔두거나 주석처리 수준, 
-- 이미 있는 컬럼 삭제는 운영 중 에러가 날 수 있으므로 유지만 합니다.
